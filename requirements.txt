# VoxelForge Python dependencies
# ================================

# Core
numpy>=1.24.0

# REST API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# HTTP client (used by AI tools layer to call the API)
requests>=2.31.0

# AI integration (optional — required only for LLM-powered agent)
# Install with: pip install openai
openai>=1.20.0

# Development / testing
pytest>=8.0.0
httpx>=0.27.0          # async test client for FastAPI
